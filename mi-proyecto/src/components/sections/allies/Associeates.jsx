const Associates = ({src, alt, title}) => {
    return(
        <li>
            <img
               src={src}
               alt={alt}
               title={title}
            />
        </li>
    )
}

export default Associates;